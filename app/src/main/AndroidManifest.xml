<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.covid19break.afinal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".india_cases"></activity>
        <activity android:name=".assamcases" />
        <activity android:name=".helpline_numbers" />
        <activity android:name=".notifications" />
        <activity android:name=".official_news" />
        <activity android:name=".trackcases" />
        <activity android:name=".precautions_tour" />
        <activity android:name=".about_us" />
        <activity android:name=".termsofuse" />
        <activity android:name=".helpline" />
        <activity android:name=".symptoms" />
        <activity android:name=".spreads" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>